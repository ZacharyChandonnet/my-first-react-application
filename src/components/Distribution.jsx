import Personne from "./Personne";

const Distribution = ({personne, distribution}) => {

    return (
        <Personne personne={personne} distribution={distribution}/>
    );
}

export default Distribution;